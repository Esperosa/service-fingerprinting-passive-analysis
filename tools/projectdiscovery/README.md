# ProjectDiscovery tools

The prototype can call ProjectDiscovery `httpx` and `nuclei` for optional
controlled web checks.

The public repository does not store external Windows binaries or downloaded
ZIP archives. Install them from the official ProjectDiscovery releases if you
want to run those optional checks locally.

Checksum files from the local verification environment are kept for traceability.
